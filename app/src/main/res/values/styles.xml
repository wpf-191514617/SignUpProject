<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowActionBar">false</item>

        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:windowIsTranslucent">true</item>-->
        <!--<item name="android:windowContentOverlay">@android:color/transparent</item>-->
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="actionMenuTextColor">@color/colorAccent</item>
        <item name="popupMenuStyle">@style/popMenu_style</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="popMenu_style" parent="@android:style/Widget.PopupMenu">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="CustomCheckBoxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/bg_check_box</item>
    </style>


    <style name="ActionSheetDialogStyle" parent="android:Theme.Dialog">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--无标题栏-->
        <item name="android:windowActionBar">false</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="BottomDialog_Animation" parent="Animation.AppCompat.Dialog">
        <item name="android:windowEnterAnimation">@anim/tranlate_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/tranlate_dialog_out</item>
    </style>

    <style name="CenterDialog_Animation" parent="Animation.AppCompat.Dialog">
        <item name="android:windowEnterAnimation">@anim/gradient_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/gradient_dialog_out</item>
    </style>


    <declare-styleable name="CountDownButton">
        <attr name="count_time" format="integer" />
        <attr name="text_color" format="color" />
        <attr name="start_text_color" format="color" />
        <attr name="load_text_color" format="color" />
        <attr name="start_back" format="reference" />
        <attr name="load_back" format="reference" />
        <!--开始的文字-->
        <attr name="start_text" format="string" />
        <attr name="end_text" format="string" />
    </declare-styleable>

    <style name="DefaultDialog" parent="android:style/Theme.Dialog">
        <item name="android:background">#00000000</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="progress" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:progressDrawable">@drawable/bg_progress1</item>
    </style>


</resources>